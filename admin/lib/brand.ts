/**
 * Single source of truth for the product's name. Coined working mark — change
 * the two strings here and the whole UI (header, titles, login, metadata) follows.
 */
export const BRAND = {
  name: 'Sablier',
  tagline: 'Fleet desk',
} as const;
